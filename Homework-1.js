// 1. จงคำนวณค่าด้านล่างต่อไปนี้ และ คอมเม้นตอบหลังคำถาม
5 + "34"            // ตอบตรงนี้ 534
5 ** 4              // ตอบตรงนี้ 625
5 - "4"             // ตอบตรงนี้ 1
10 % 5              // ตอบตรงนี้ 0
5 % 10;             // ตอบตรงนี้ 5
(2 ** 5) % 2        // ตอบตรงนี้ 0
"Java" + "Script"   // ตอบตรงนี้ JavaScript
" " + " "           // ตอบตรงนี้   
" " + 0             // ตอบตรงนี้  0
20 + 4 + "15"       // ตอบตรงนี้ 2415
true + true         // ตอบตรงนี้ 2
true + false        // ตอบตรงนี้ 1
false + true        // ตอบตรงนี้ 1
false - true        // ตอบตรงนี้ -1
false - true + false - true + false;    // ตอบตรงนี้ -2
(true + true) ** 4  // ตอบตรงนี้ 16
19 + true + "20"    // ตอบตรงนี้ 2020
3 - 4               // ตอบตรงนี้ -1
"Bob" - "bill"      // ตอบตรงนี้ NaN
"Code" + "Camp" + true + false + null + undefined   // ตอบตรงนี้ CodeCamptruefalsenullundefined


// 2. จงคำนวณค่าเปรียบเทียบต่อไปนี้ และ คอมเม้นตอบด้านหลังเหมือนข้างบน
5 >= 1          // true
0 === 1         // false
4 <= 1          // false
1 != 1          // false
1 == "1"        // true
1 === "1"       // false
"2" == "2"      // true
"2" === "2"     // true
"12" !== 12     // true
"15" != 15      // false
"A" > "B"       // false
"B" < "C"       // true
"a" > "A"       // true
"b" < "A"       // false
"bee" > "bbe"   // true
"BeE" < "Bee"   // true
"SonTer" > "Soncom"     // false
"circle" > "circles"    // false
"NaRuTo" < "nArUtO"     // true
true === false  // false
true != true    // false


// 3. สร้าง String ที่มีค่า "Hello, It's me. "Mario"."
`"Hello, It's me. "Mario"."`